node_modules/
dist/
.DS_Store
*.local
.claude/
.vite/
snap/
cfg/

# Playwright
test-results/
playwright-report/

# ROM files — never commit copyrighted material
public/*.zip
public/roms/
**/public/roms/
**/public/*.zip
.rom-cache/

# pi-gen residue from the earlier Phase 5 iteration (now archived —
# the active workflow is Raspberry Pi Imager + first-boot.sh). Keep
# the ignore entries so a local leftover checkout never lands in git.
packages/sprixe-image/pi-gen/
*.img
*.img.xz
